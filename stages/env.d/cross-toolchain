#
# Cross toolchain variables for the environment
#

BTCC="${target}-gcc"
BTCXX="${target}-g++"
AR="${target}-ar"
AS="${target}-as"
LD="${target}-ld"
RANLIB="${target}-ranlib"
READELF="${target}-readelf"
STRIP="${target}-strip"

