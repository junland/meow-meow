echo "Making minimal directory layout on 'rootdir' ..."

mkdir -pv $rootdir/{etc,var,usr}

for i in bin lib lib64 sbin; do
  mkdir -pv $rootdir/usr/$i
  ln -sv usr/$i $rootdir/$i
done

mkdir -pv $rootdir/tools